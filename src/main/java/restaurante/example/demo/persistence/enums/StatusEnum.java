package restaurante.example.demo.persistence.enums;

// Enum que define los posibles estados de reserva de una mesa.
public enum StatusEnum {
      DISPONIBLE,  // Mesa disponible para ser ocupada
      OCUPADA,    // Mesa ocupada
      RESERVADA   // Mesa reservada para un futuro cliente
}